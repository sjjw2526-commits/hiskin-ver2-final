# 작업기록

날짜순으로 무엇을 왜 그렇게 했는지 남깁니다.
"왜"가 없으면 나중에 같은 실수를 되풀이하게 되므로, 판단 근거를 같이 적습니다.

배포처: **https://hiskin.netlify.app** · 배포 명령: `npm run deploy`

---

## 2026-08-19 ~ 08-20

### 이날 올라간 것

| 커밋 | 내용 |
|---|---|
| `c03c98e` | HISKIN B2B 원페이지 최초 구축 |
| `9b0a94c` | 정적 파일로 빌드되게 전환 (`output: "export"`) |
| `2a60c45` | Netlify 연동 + 문서화 |
| `7b9efce` | 제조·연구 섹션 (공장 사진 4장) |
| `809a3bd` | 임상 데이터 + 공인 인증서 섹션 |
| `91cd28a` | 임상 수치를 성적서 실측값으로 교체, 인증서 스캔 5장 |
| `aab931f` | 이미지 로드 실패 시 재시도 (버그 수정) |

---

### 1. 제조·연구 섹션 (`Manufacturing.tsx`)

공장 사진 4장 + 생산 규격 4가지. 네비게이션에는 넣지 않았습니다.

**타일을 4:3 대신 16:9로 했습니다.**
받은 CSS는 `aspect-ratio: 4/3` 이었지만 사진 4장이 전부 1920×1080(16:9)이었습니다.
4:3으로 자르면 좌우 25%가 날아가는데, 어디가 잘리는지 표시해서 확인해보니
2번 사진은 오른쪽 설비가, 4번은 좌우 탱크가 잘려나갔습니다. 크롭 손실 0으로 두고
대신 우측 그리드 폭을 넓혀 균형을 맞췄습니다.

**모바일은 1열입니다.** 2열이면 타일이 158px까지 줄어 설비가 안 보입니다.

원본 PNG 4장 6.1MB → JPEG 413KB.

---

### 2. 임상 데이터 섹션 (`ClinicalData.tsx`)

세명대 화장품임상연구센터 성적서 2건의 수치를 차트 2개로 보여줍니다.

**처음엔 평균·표준편차만 받은 상태여서**, 피험자 10명 점을 평균 69.0 / 표준편차 9.4를
정확히 재현하도록 만든 **분포 예시**로 넣고 그렇다고 명시해뒀습니다.

**이후 성적서 PDF 원본을 찾아 전부 실측값으로 교체했습니다.**
PDF에 암호가 걸려 있어 `pdfjs-dist` 로 열었고, Appendix I 에 원문 표가 있었습니다.
검산 결과 평균 69.03(→69.0), 표준편차 9.40, 평균 ITA 49.3° 로 요약값과 일치했습니다.

받은 원고의 시험번호 `SMC-260731-9090 EN`(띄어쓰기)은 오타였고, 원문은 `_EN`(언더바)입니다.

**아직 모식도인 것 — UVA 흡광 곡선.**
성적서에 플레이트별 임계파장(λc)만 있고 흡광 스펙트럼 원본이 없습니다.
그래서 곡선은 λc 377.6nm 조건(290→377.6 면적 = 290→400 전체의 90%)으로 역산한
모식도이며, **화면에도 "모식도"라고 적어두고 실측 4장 수치는 표로 따로** 보여줍니다.
분광광도계 원본이 생기면 `CURVE_PATH` 상수 하나만 바꾸면 됩니다.

**차트는 라이브러리를 안 썼습니다.** 보여줄 게 숫자 두 개라서, 범용 차트를 쓰면
축·범례·격자가 딸려와 페이지 톤과 싸웁니다.

**왼쪽 카드가 비어 보이던 문제** — 오른쪽 카드에 표가 생기면서 높이가 벌어졌습니다.
성적서에 있던 ITA° 구간 분포표를 넣어 채웠습니다. 여백 메우기용이 아니라
ISO 24444 패널 요건을 충족했다는 근거라 바이어 실사팀이 보는 항목입니다.

---

### 3. 공인 인증서 섹션 (`Certifications.tsx`)

서류 5종, 클릭하면 라이트박스로 크게 열립니다.

**ISO 인증서 원본이 90° 돌아가 있었습니다.** 회전 4방향을 다 뽑아 비교해 바로잡았습니다.

**5장 비율이 제각각(1.33 / 1.41 / 1.42 / 1.49)이라 A4로 통일했습니다.**
잘라내지 않고 흰 여백을 덧대는 방식(1500×2121)이라 손실이 없고, 타일 5개가
똑같이 채워집니다. 재변환은 `node scripts/build-certs.mjs`.

**PDF 다운로드 버튼은 파일이 실제로 있을 때만 뜹니다.**
`public/docs/` 에 PDF가 없으면 버튼 자체가 렌더되지 않습니다(HEAD 요청으로 확인).
깨진 링크를 5개 뿌리는 것보다 낫습니다. 현재 인증서 PDF는 없는 상태입니다.

**⚠️ 명의가 셋으로 갈립니다 — 확인 필요**

| 서류 | 명의 |
|---|---|
| ISO 22716 | 주식회사 지디엠 (충북 청주) |
| 제조업 등록필증 | 에스엘코스메틱(주) (경기 시흥) |
| 기업부설연구소 | 에스엘코스메틱(주) |
| 특허 | 개인 (이인철) |
| 성적서 의뢰인 | GDM Co., Ltd. |

바이어 실사팀이 반드시 확인하는 부분입니다. 임의로 하나로 합치지 않고
라이트박스에 **"명의" 항목을 따로 만들어 그대로 표시**했습니다.
(특허증은 이인철, 등록필증 대표는 이민철인데 생년월일이 둘 다 700508로 같습니다)

---

### 4. 이날 잡은 버그

**① 가로 캐러셀이 로드 직후 24px 밀림**
`padding` 으로 여백을 주면 스크롤 범위가 화면 끝까지 남아 있어서 스냅이 레일을
자기 여백만큼 밀어냅니다. `margin` 으로 바꿔 해결. 리뷰 캐러셀에서 이미 겪은 문제입니다.

**② 이미지 로드가 한 번 실패하면 영구히 자리표시로 고정**
배포 직후 확인했더니 인증서 5장이 전부 회색으로 떴습니다. 파일은 200으로 정상 응답하는데도요.
CDN 엣지가 차가울 때 첫 요청이 실패하면 `failed` 상태가 고정되는 구조였습니다.
새로고침하면 정상이지만 **처음 들어온 방문자는 그 세션 내내 인증서를 못 봅니다.**
인증서를 보여주는 게 목적인 페이지라 치명적이라 고쳤습니다.

400ms/800ms 백오프로 2회 재시도 후에만 자리표시로 넘어갑니다. 두 방향 다 검증:
- 첫 요청을 강제로 끊음 → `?r=1` 로 복구, 자리표시 0개
- 진짜 없는 파일 → 정확히 3번만 시도하고 중단 (무한 재시도 없음)

재시도 횟수를 state가 아니라 ref에 둔 이유: setState 갱신함수 안에서 타이머를 걸면
StrictMode에서 두 번 실행됩니다.

배포 직후 라이브 확인 결과 **5장 전부 `?r=1` 로 로드**됐습니다. 이번에도 콜드 엣지
실패가 났고 재시도가 다 잡아냈습니다.

---

### 5. 리뷰 영상 1번째 (2번 슬롯)

받은 `동영상 1.mp4` 를 2번 자리에 넣었습니다. 1080×1920 9:16 / 18초로 규격은 맞았지만
**그대로 넣으면 안 되는 문제가 두 개** 있었습니다.

**① 코덱이 H.265(hvc1)였습니다.** 아이폰 기본 저장 포맷인데 크롬·파이어폭스에서
재생이 안 됩니다. `VideoGrid` 는 재생 실패를 감지해 자리표시로 바꾸므로,
"영상이 안 뜬다"가 아니라 **"조용히 회색 카드가 된다"** 로 나타났을 겁니다. → H.264로 재인코딩.

**② HDR(HLG / BT.2020)이었습니다.** 톤매핑 없이 변환하면 일반 모니터에서 색이 바랩니다.
변환 전후 프레임을 나란히 놓고 확인했더니 차이가 뚜렷해서, BT.709 SDR로 톤매핑했습니다.

**해상도는 810×1440으로 낮췄습니다.** 카드가 화면에서 제일 클 때가 384px라 1080은
용량만 두 배가 됩니다. CRF 23(4.47MB)과 26(3.13MB)을 제품 라벨 글씨가 보이는
1:1 크롭으로 비교했더니 육안 차이가 없어 **26으로 확정**했습니다. 최종 3.28MB.

같은 작업을 나머지 7개에도 해야 하므로 스크립트로 만들었습니다:
`npm run video -- "<원본파일>" <슬롯번호>`

**썸네일(poster) 지정 기능을 넣었습니다.** 원래 `VideoCard` 에 poster가 없어서 재생 전
카드가 비어 보였습니다. 요청하신 컷에 맞춰 3.4~4.1초를 뽑아 비교했고 — 4.1초는 눈을
감고 있어서 — **3.7초**로 정했습니다. 변환 스크립트가 썸네일도 같이 뽑습니다.

**소리는 "문제 없다"고 하셔서 그대로 뒀습니다.** 참고로 원인은 모달 영상의 `loop` 로,
18초마다 배경음악이 다시 시작됩니다. 카드는 음소거라 조용하고, 모달을 닫으면 소리는
정상적으로 멈춥니다(확인함). 거슬리면 `VideoGrid.tsx` 모달 `<video>` 에서 `loop` 만 빼면 됩니다.

**⚠️ 계정명이 지어낸 것입니다.** 2번 카드에 실제 인물이 나오는 영상이 들어갔는데
표시되는 계정은 `@sunny.beautylog` 로 자리표시입니다. 실제 촬영자 계정으로
바꿔야 합니다. 문구는 영상 내용에 맞게 "하이스킨 데일리 선크림 제품 소개"로 고쳤습니다.

---

### 6. 남은 것

- [ ] **문의 폼 전송** — 화면상 성공 표시만 뜨고 실제로 메일이 가지 않습니다.
      **거래처에 주소를 알리기 전에 반드시 처리해야 합니다.** (`InquiryForm.tsx` 의 `handleSubmit`)
- [ ] 인증서 PDF 5개 — 없음. 생기면 `public/docs/` 에 넣으면 버튼이 자동으로 붙습니다
- [ ] UVA 흡광 곡선 원본 데이터 (위 2번 참고)
- [ ] `img-02` ~ `img-10` — 아직 자리표시
- [ ] 리뷰 영상 7개 + **계정명·문구** — 2번 슬롯만 실제 영상입니다
- [ ] 페이지에서 문구를 직접 고치는 편집 모드

---

## 참고

- 프로젝트 전반·배포·수정 방법 → [`README.md`](README.md)
- 이미지 파일명·규격 → [`public/images/README.md`](public/images/README.md)
- 인증서 PDF 넣는 법 → [`public/docs/README.md`](public/docs/README.md)
- **스크롤 모션은 건드리기 전에 README 7번 항목을 꼭 읽으세요.** 서로 맞물려 있어서
  이유를 모르고 고치면 예전에 잡은 문제가 되살아납니다.
