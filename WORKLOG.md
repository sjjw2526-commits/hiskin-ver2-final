# 작업기록

날짜순으로 무엇을 왜 그렇게 했는지 남깁니다.
"왜"가 없으면 나중에 같은 실수를 되풀이하게 되므로, 판단 근거를 같이 적습니다.

배포처: **https://hiskin.netlify.app**
저장소: **https://github.com/sjjw2526-commits/hiskin-ver2-final** (Private)

**`git push` 하면 Netlify가 알아서 배포합니다.** (master 브랜치 연결됨)
급할 때 로컬에서 바로 올리려면 `npm run deploy` 도 그대로 씁니다.

---

## 2026-08-24 — 모바일 전면 손질

390px 로 처음부터 끝까지 훑어보고(스크린샷 23컷) 어색한 곳을 전부 잡았습니다.
데스크톱은 건드리지 않았습니다 — 모든 수정이 `md` 미만에만 걸립니다.

### 1. 2페이지 제품 3장 세로 나열 (`Hero.tsx`)

390px 에서 `grid-cols-3` 한 칸이 **폭 122px × 높이 500px** 짜리 세로 조각이 됐습니다.
어깨 사진은 목선만, 핑크 텍스처는 글씨가 반쯤 잘려 보였습니다.
→ 모바일 `grid-cols-1` + 각 칸 `aspect-[4/5]`. md 부터는 기존 `h-[60vh] grid-cols-3` 그대로.

### 2. 축소 모션 모바일에서 제거 (`Hero.tsx`)

**왜**: 모바일은 헤드라인이 작아 착지 슬롯이 1.4em 밖에 안 됩니다. 그래서 화면 전체 사진이
**빈 회색 화면 한가운데 떠 있는 작은 조각**으로 한참 머무는 구간이 생겼습니다.
데스크톱 전용 연출인데 모바일에 그대로 돌고 있었던 것.

`gsap.matchMedia()` 로 분기했습니다. 단순 `if (isPhone)` 이 아니라 matchMedia 인 이유:
가로/세로 회전으로 768px 을 넘나들면 레이아웃 클래스는 바뀌는데 JS 는 처음 판단을 그대로
들고 있어서 어긋납니다. matchMedia 는 경계를 넘을 때 이전 설정을 되돌리고 다시 깔아줍니다.

- 모바일: 축소 스크럽·비행 티커 전부 안 검. 히어로는 러너웨이 안의 **평범한 이미지**
  (`absolute`, md 부터 `fixed`) 라 그냥 스크롤에 밀려 올라갑니다. 인라인 img-01 은 처음부터 1.
- img-09 는 날아오지 않고 가운데 칸에 그냥 놓습니다. "Zero [ ] Foundation" 의 빈 슬롯도
  모바일에선 `w-0` 으로 닫아 낱말 사이 공백처럼 보이게.
- 잔상 방어 로직(`enforceHandover` 등)은 데스크톱 전용. 모바일은 고정 요소가 아니라
  화면에 남을 수가 없습니다.

**주의**: 분기하면서 "SCROLL" 페이드아웃 스크럽까지 데스크톱 블록에 넣었다가 모바일에서
표시가 페이지 내내 안 사라졌습니다(스크린샷으로 잡음). 창에 고정된 요소라 양쪽 다 필요 —
matchMedia 바깥으로 뺐습니다.

### 3. 스크롤 버벅거림 (`SmoothScroll.tsx` + vh → svh)

원인 셋을 같이 잡았습니다.

- **Lenis 를 768px 미만에서 끔.** 터치는 이미 관성 스크롤이고, 드래그는 휠 델타가 아니라
  페이지 자체를 움직여서 두 이징이 같은 제스처를 두고 경합합니다. ScrollTrigger 는
  네이티브 스크롤로도 그대로 돕니다.
- **매 프레임 rect 3회 + 전체화면 scale** 이 사라짐 (위 2번).
- **vh → svh**: 모바일 `vh` 는 주소창이 접힌 상태 기준이라, 주소창이 접히면 높이가 변하고
  그 아래 트리거가 전부 다시 계산되며 덜컥거립니다.
  `Hero` 러너웨이 `h-[100svh]`, `min-h-svh`, `h-[5svh]`, `Philosophy` `h-[80svh]`,
  `Certifications` 모달 `h-[50svh]`.

### 4. 모바일 여백 축소

데스크톱 기준 `py-24`(96px) 가 모바일에 그대로 걸려 섹션 사이가 화면 40% 씩 비었습니다.
8개 섹션 `py-24 → py-16`, 제목 아래 여백도 같이 줄임. `md:py-36` 은 손대지 않음.

### 5. 어깨 사진 중복 (`ArchiveGallery.tsx`)

img-02(어깨)가 히어로 트리오와 아카이브 우하단에 **둘 다** 있었습니다. 모바일에서 세로로
쭉 보게 되니 반복이 티가 납니다. 아카이브 쪽을 새 컷으로 교체.

**어떤 걸 골랐나** — 16:9 가로 컷이 필요한데 새로 만든 컷(엘리베이터·비치타월·핑크음료)은
전부 세로라 못 씁니다. 안 쓰이던 기존 에셋에서:

- `img-17`(핑크 줄무늬 타월+튜브 2개) → **탈락**. 뒤집힌 왼쪽 튜브가 **"SPF 60+ / PA+++++"**
  로 적혀 있습니다(원본 에셋의 AI 오타). 사이트 전체가 SPF 50+ 이고 임상 섹션이 표기 기준
  얘기라 이건 못 올립니다.
- `img-16`(밀짚모자 챙 너머 햇살 + 튜브, 무지개) → **채택**. 표기 정상, 구도 좋음.
  16:9 로 잘라 `img-21.jpg` 로 저장(1792×1008, 123KB). 원본 img-16 은 그대로 둠.

### 6. UVA 흡광 곡선 라벨 겹침 (`ClinicalData.tsx`)

"UVA 320–400NM" 과 "λc = 377.6nm" 이 **곡선 위에 그대로 인쇄**돼 있었습니다.
곡선이 왼쪽 위에서 평평하다가 오른쪽으로 떨어지는 모양이라 밴드 위쪽은 항상 곡선이 지납니다.
→ 둘 다 곡선이 비워두는 **왼쪽 아래 쐐기**로 옮김(밴드 라벨 `y=CURVE_H-26`,
λc 라벨 `y=LC_Y+30`). **모바일만의 문제가 아니라 데스크톱도 같은 상태였습니다.**

### 검증

- 모바일 390px 전체 스윕 23컷 (수정 전/후 비교), `scrollWidth` 394 → 390 (가로 넘침 사라짐)
- 데스크톱 1905px 히어로 축소 시퀀스 8프레임 — 착지·핸드오버 정상
  (`fixedOpacity 0 / inlineOpacity 1`), 트리오·캡션 정상
- UVA 차트 390px / 1905px 각각 확인 (애니메이션 끝난 뒤 촬영해야 함 — 중간에 찍으면
  곡선이 그리다 만 것처럼, 카운터가 0.00 으로 보입니다)

### 아직 남은 것

- 히어로 튜브 라벨이 **"SPF 90+"** 로 보임 (사진 자체 문제, 재생성 필요 — 힉스필드 MCP
  연결이 끊겨 이번엔 못 함)
- 만들어 둔 엘리베이터·비치타월·핑크음료(일반/화보) 컷은 아직 자리 미정

---

## 2026-08-21 (회사 PC, 저녁)

### 히어로 잔상 버그 2차 — "로딩 직후 바로 스크롤" (`Hero.tsx`)

**증상**: 인트로가 끝나자마자 스크롤을 내리면 히어로 사진(img-01)이 작게 축소된 채
트리오 가운데 칸(img-09) 위에 남음. 하단 "↓ SCROLL" 표시도 같이 남음. 천천히 내리면 안 생김.

**원인**: 오후 수정(#4)은 *인트로가 끝난 시점*에 이미 아래에 있는 경우만 막았습니다.
이번엔 **경합**입니다. 인트로가 끝나면 히어로를 **1초 동안 페이드인하는 트윈**이 도는데,
그 1초 안에 러너웨이를 벗어나면 `onLeave`가 투명도를 0으로 내리지만, 아직 돌고 있는
페이드인 트윈이 다음 프레임에 다시 1로 덮어씁니다. 스크럽은 끝 상태(축소)라 잔상으로 보입니다.
"SCROLL" 표시도 같은 구조(0.4초 지연 + 0.8초 페이드인 vs 스크롤 페이드아웃 스크럽)입니다.

**수정**

1. `onLeave` / `onRefresh(progress ≥ 1)` 에서 `gsap.killTweensOf(fixed, "opacity")` 로
   페이드인을 먼저 죽인 뒤 넘김. 속성을 `"opacity"` 로 한정한 이유: 같은 요소에 x/y/scale
   스크럽 트윈이 붙어 있어 `overwrite: true` 를 쓰면 그것까지 죽습니다.
2. 페이드인 트윈 `onComplete` 에서 러너웨이를 지났으면 스스로 꺼짐 (이중 안전장치).
3. "SCROLL" 페이드인은 `onUpdate` 에서 러너웨이 12% 지점을 지나면 자기를 죽이고 0으로.
4. 두 인트로 트윈 모두 effect cleanup 에서 kill.

5. 매 프레임 도는 `flight` 티커 맨 앞에서 **런웨이를 지났으면 무조건** 고정 히어로 0 /
   인라인 사본 1 을 강제. GSAP 은 트윈 렌더 → 티커 콜백 순이라 같은 프레임에서 항상
   마지막에 이깁니다. 배포 직후 라이브 16회 검증 중 첫 1회가 재현됐는데(이후 15회 정상)
   원인을 못 박지 못해, 누가 1로 되돌리든 상관없도록 넣은 최종 방어선입니다.

**검증**: Chrome 헤드리스로 `out/` 을 띄워, 페이드인이 막 시작된 시점(opacity 0.12)에
트리오 위치로 점프 → 2.5초 후 측정. 수정 전 빌드 `fixedOpacity: 1`(잔상), 수정 후 `0`.
스크립트는 일회용이라 저장소에 넣지 않았습니다.

### 아카이브 좌측 대형 → 빨랫줄 컷 (img-18 교체)

테니스 코트 모델 컷을 **맑은 하늘 아래 빨랫줄에 매달린 흰 튜브 3개** 컷으로 교체했습니다.

- 원본은 힉스필드 생성물(다운로드 `hf_20260816_155328_…png`, 세로 3:4, 튜브 아이보리).
- 1차: nano_banana_pro 로 **튜브만 아이보리 → 흰색**, 노란 필터 제거. 모양·글씨·블루 포인트 유지.
- 아웃페인트(outpaint) 도구로 4:3 확장은 **실패** — 튜브가 작아지고 빈 집게·구름이 추가되고
  캡 글씨가 깨짐. 이 도구는 구도 유지가 안 되니 다음에도 쓰지 말 것.
- 2차: 1차 결과를 참조로 넣고 **4:3 으로 직접 재생성**. 튜브 3개 온전, 글씨 정상. 이걸 채택.
- 칸 비율: 데스크톱 좌측 칸은 우측 컬럼 높이를 따라 약 4:3, 모바일은 16:10. 2400×1792 JPG(q86, 237KB).
- 깨알 설명 글씨는 원본부터 AI 오타가 있음(확대해야 보임). 메인 문구는 정상.

### SPF 50+ 문구 → 오른쪽 하단으로 이동 (`Philosophy.tsx`)

새 img-03 은 왼쪽 아래가 흰 상의라 흰 글씨 대비가 약했음. 오른쪽 아래는 어두운 초록 보케라
잘 읽히고, 모델 시선 방향과도 맞음. `left-6 md:left-[60px]` → `right-6 md:right-[60px] text-right`.
등장 애니메이션은 y축만 써서 영향 없음. 렌더에 CSS 주입해 데스크톱·모바일 미리 본 뒤 적용.

### 철학 섹션 SPF 50+ 풀블리드 → 역광 프로필 컷 (img-03 교체)

눈 감은 얼굴 클로즈업을 **히어로 모델(img-01)을 참조로 생성한 역광 옆얼굴 컷**으로 교체.
참고로 받은 사진은 워터마크 있는 실제 인물 사진이라 쓰지 않고 구도·빛만 참고함.

- nano_banana_pro, img-01 을 image_references 로 넣어 16:9 2장(A: 얼굴 더 보임 / B: 뒤에서 본 옆모습).
- 둘 다 실제 블록에 끼워 데스크톱 1905 · 모바일 390 렌더 비교 → **B 채택**.
  이유: 모바일 object-cover 중앙 크롭에서 A는 귀만 남고 얼굴이 잘림, B는 얼굴이 남음.
- 아래 1/3 어둡게 규칙은 못 지킴(흰 상의). 기존 `from-black/45` 그라데이션으로 흰 글씨는
  읽히는 것 확인. 더 필요하면 그라데이션을 /55 로 올리면 됨.
- 2400×1340 JPG(q86, 249KB). A 는 `Downloads/hiskin_역광프로필_16x9_A.png` 에 보관.

---

## 2026-08-21 (회사 PC, 오후)

아카이브 갤러리를 새 사진으로 갈아끼우고, 제품 상세 사진을 재생성하고, 글씨를 전반적으로 키웠습니다.

### 1. 아카이브 갤러리 — 중복 사진 정리 (3/4 완료)

아카이브 4장이 전부 위쪽 섹션과 중복이었습니다(img-09/10/02는 히어로 트리오와
같은 파일, img-08은 제품 상세와 같은 촬영본). 히어로를 건드리지 않기 위해
파일을 덮어쓰지 않고 **새 슬롯 번호를 만들어** 교체했습니다.

| 슬롯 | 이전 | 현재 |
|---|---|---|
| 좌측 대형 (가로) | img-08 | **img-18** — 테니스 코트에서 제품 든 모델. 세로 원본(1760×2352)을 얼굴~가슴선 가로 4:3 밴드로 크롭 |
| 우상단 좌 (3:4) | img-09 | **img-19** — 보그 매거진 + 니트 백 플랫레이 (컨셉샷 2.jpg 그대로, 비율 일치) |
| 우상단 우 (3:4) | img-10 | **img-20** — 화이트 테이블 튜브+박스 플랫레이 (컨셉샷 4.jpg, 9:16 → 3:4 크롭) |
| 하단 와이드 (16:9) | img-02 | **아직 img-02 (미교체)** — 히어로 트리오와 여전히 중복. 다음에 교체할 것 |

### 2. 제품 상세 img-15 — 재생성 (2880×2160)

제미나이로 만든 세로 컷(기울어진 튜브)을 받아 아웃페인팅으로 4:3을 만들었는데
세 가지 문제가 나왔습니다: ① 우하단 제미나이 별표 워터마크 ② 튜브 글씨 번짐
③ 실물감 부족. 지우는 걸로는 해결이 안 돼 **실물 튜브 사진을 레퍼런스로 걸고
같은 구도(기울어진 튜브 + 삼단 분홍 제형)를 재생성** → 2K 업스케일했습니다.
큰 글씨(HISKIN / Daily Suncream Protect / SPF 50+ PA++++)는 전부 정확하고,
깨알 문단은 여전히 AI 특유의 뭉개짐이 있습니다(화면에선 안 보임, 확대·인쇄 시 후보정 필요).

### 3. 글씨 크기 일괄 확대

- 철학 섹션 헤드라인: 화면폭 2.9% → 3.5% (1920px 기준 약 56→67px), 줄간격 1.16 → 1.3
- 철학 우측 루틴: 이니셜 30→36px, 제목 22→26px, 설명 17→19px, 안내문 18→20px
- 제품 상세 스펙: 항목명 16→18px, 값 21→25px

### 발견했지만 안 고친 것

- 🔴 (그대로) 히어로 img-01 "SPF 90" 오표기 — 바이어 공유 전 필수 수정
- 🟡 아카이브 하단 와이드가 아직 히어로와 중복 (img-02) — 4번째 사진 대기 중
- 🟡 제품 상세 레이아웃이 철학 섹션과 같은 "좌측 덩어리 + 우측 리스트" 반복이라는
  피드백 있었음 — 레이아웃 개편 논의 중단 상태 (사진 문제 먼저 처리하느라)
- 🟡 다운로드의 `선크림 표준.jpg`(5712×4284 가로 고해상도)는 아카이브 와이드 후보로 보류

---

## 2026-08-20 (회사 PC, 오후)

이미지 4장 · 리뷰 영상 5개를 채우고, 히어로 잔상 버그를 잡았습니다.

### 1. 히어로 트리오 + 철학 섹션 이미지

| 슬롯 | 들어간 것 |
|---|---|
| `img-02` | 도시 햇빛 아래 맨 어깨·목선 |
| `img-03` | 햇빛 아래 눈 감고 미소 짓는 얼굴 |
| `img-09` | 핑크빛 물속의 제품 (실물 사진을 레퍼런스로 생성) |
| `img-10` | 벚꽃 + 연분홍 제형에 새긴 HISKIN 글씨 |

전부 AI 생성입니다. 채택 안 된 후보들은 `Downloads/hiskin_img*` 에 남겨뒀습니다.

**이 슬롯들의 진짜 제약은 "한 파일이 여러 비율로 잘린다"는 것입니다.**
`img-02` 는 트리오 3:5 세로와 아카이브 16:9 가로에 같은 파일이 들어가고,
`img-09` 는 거기 더해 문장 속 글자 크기(폭 20~30px)로도 나옵니다.
**가장자리에 중요한 것을 두면 어느 한쪽에서 반드시 잘립니다.**
넣기 전에 세 비율로 잘라보고 확인하는 편이 빠릅니다.

`img-10` 의 HISKIN 글씨는 3:5 로 자르면 H 와 N 이 잘렸습니다. 프롬프트로 글씨를
작게 만들라고 해도 모델이 안 따라서, **캔버스를 세로로 넓혀서**(아웃페인팅)
글씨가 상대적으로 작아지게 했습니다. 세 크롭 다 온전히 나옵니다.

`img-03` 은 **아래 1/3이 어두워야 합니다.** 그 위에 흰 `SPF 50+` 가 최대 7.5rem 으로
얹히는데 밝은 배경이면 안 읽힙니다. 그늘이 아니라 조명 방향으로 해결했습니다.

### 2. 리뷰 영상 5개 (`review-01`, `03`~`06`)

| 슬롯 | 원본 | 성격 |
|---|---|---|
| 01 | 소민쌤 1 (73초) | 실촬영 |
| 03 | 민영쌤 2 (75초) | 실촬영, HDR |
| 04~06 | AI 생성 3개 | 15초 / 15초 / 5초 |

### 3. `build-video.mjs` 버그 2개 + 옵션 1개

이 스크립트는 **아이폰 HDR 영상 하나를 넣던 때 기준**으로 만들어져 있었습니다.

**(1) 톤매핑이 무조건 걸렸습니다.** 필터 체인이 HLG 전용인데 이번 6개 중 HLG 는
`민영쌤 2` 하나뿐이었습니다. 나머지는 이미 SDR 이라 그대로 걸었으면 한 번 더 눌려서
색이 다 빠졌을 겁니다. → 원본 색공간을 읽어 **HDR 일 때만** 톤매핑합니다.

**(2) `scale=810:1440` 이 비율을 무시했습니다.** 세로 영상은 9:16 에 가까워 티가 덜
났지만 조금씩 늘어나고 있었고, 가로 영상은 아예 찌그러집니다.
→ `force_original_aspect_ratio=increase` + `crop` 으로 잘라서 채웁니다.

**(3) 거울글씨 → `flip` 옵션 추가.** `소민쌤 1`(셀피가 좌우 반전으로 저장됨)과 AI 영상
하나에서 튜브의 `HISKIN` 이 뒤집혀 있었습니다. 프레임 전체를 뒤집으면 브랜드명이 바로
읽힙니다. 화면에 다른 글자가 있으면 그쪽이 대신 뒤집히니 **결과를 눈으로 확인하세요.**

사용법: `node scripts/build-video.mjs "<원본>" <슬롯> [썸네일초] [flip]`

### 4. 히어로 잔상 버그 (`Hero.tsx`)

**증상**: 페이지 중간(제품 상세쯤)에 히어로 사진이 작게 떠 있음.

**원인**: 인트로가 끝나면 히어로를 나타내는 코드가 **페이지가 어디에 있는지를 안 봤습니다.**
무조건 투명도를 1로 올렸습니다. 정상 흐름에서는 로딩 중 스크롤이 잠겨 있어(`html.is-loading`)
항상 맨 위라 문제가 없었는데, **아래로 내려간 상태에서 인트로가 다시 도는 경우**가 있습니다.

- 아래로 스크롤한 상태에서 **새로고침** → 브라우저가 스크롤 위치를 복원합니다
- 개발 중 **파일 저장** → Fast Refresh 로 프리로더가 다시 마운트됩니다

스크럽은 이미 끝나 있어 transform 은 "글자 사이로 축소된" 최종값인데 투명도만 1로
돌아온 것이라, 화면 한복판에 잔상처럼 보였습니다.

**수정 2가지**

1. 인트로 페이드인 전에 러너웨이를 지났는지 확인 → 지났으면 헤드라인 속 사본에 바로 넘김
2. `onRefresh` 추가 — `onLeave`/`onEnterBack` 은 **경계를 넘는 순간에만** 불립니다.
   리프레시나 앵커 점프로 건너뛰면 상태가 어긋난 채 남습니다. 리프레시마다 재적용하되
   **숨기는 쪽만** 강제합니다. 보이게 하는 것까지 넣으면 인트로 페이드를 덮어씁니다.

README 7번의 규칙들(transform 으로만 축소 / 네비 아래 기준 / scrub 지연 금지)은
건드리지 않았습니다. 투명도 전환만 손댔습니다.

### 5. `ScrollTrigger.refresh()` 누락 (`SmoothScroll.tsx`)

로드 완료 후 리프레시를 부르는 곳이 **코드 전체에 없었습니다.** 트리거는 만들어질 때
측정되는데, 아래쪽 이미지가 늦게 로드되면 페이지 높이가 밀려서 그 아래 트리거들이 전부
어긋난 위치를 겨냥합니다. Lenis 이펙트와 따로 둔 이유는, 그쪽은 "동작 줄이기" 설정이면
일찍 반환하는데 측정이 어긋나는 건 그 설정과 무관하게 일어나기 때문입니다.

### 6. 발견했지만 **아직 안 고친 것**

- 🔴 **히어로 `img-01` 라벨에 `SPF 90` 이라고 적혀 있습니다.** 실제는 SPF 50+ 입니다.
  AI 가 만들어낸 글씨인데, 사이트 최상단 제일 큰 이미지에 잘못된 스펙이 박혀 있습니다.
  **거래처에 주소를 알리기 전에 반드시 교체해야 합니다.**
- 🟡 **모바일에서 히어로 트리오가 세로 띠 3개가 됩니다.** `grid-cols-3` 에 모바일 대응이
  없어 390px 폰에서 각 칸이 약 122×820px(1:6.7)입니다. 사진을 알아볼 수 없습니다.
  의도한 디자인인지 판단 필요.
- 🟡 **리뷰 영상 01·03 이 19MB, 22MB 입니다.** 카드는 `preload="metadata"` 라 첫 로딩엔
  지장 없지만 재생하면 그만큼 받습니다. 70초가 넘는데 후기 카드는 15~20초면 충분합니다.
- 🟡 **계정명·문구가 전부 지어낸 자리표시입니다.** AI 생성 영상이 실제 고객 후기처럼
  보이는 상태이기도 합니다. 주소 알리기 전에 정리 필요.

---

## 2026-08-19 ~ 08-20

### 이날 올라간 것

| 커밋 | 내용 |
|---|---|
| `c03c98e` | HISKIN B2B 원페이지 최초 구축 |
| `9b0a94c` | 정적 파일로 빌드되게 전환 (`output: "export"`) |
| `2a60c45` | Netlify 연동 + 문서화 |
| `7b9efce` | 제조·연구 섹션 (공장 사진 4장) |
| `809a3bd` | 임상 데이터 + 공인 인증서 섹션 |
| `91cd28a` | 임상 수치를 성적서 실측값으로 교체, 인증서 스캔 5장 |
| `aab931f` | 이미지 로드 실패 시 재시도 (버그 수정) |

---

### 1. 제조·연구 섹션 (`Manufacturing.tsx`)

공장 사진 4장 + 생산 규격 4가지. 네비게이션에는 넣지 않았습니다.

**타일을 4:3 대신 16:9로 했습니다.**
받은 CSS는 `aspect-ratio: 4/3` 이었지만 사진 4장이 전부 1920×1080(16:9)이었습니다.
4:3으로 자르면 좌우 25%가 날아가는데, 어디가 잘리는지 표시해서 확인해보니
2번 사진은 오른쪽 설비가, 4번은 좌우 탱크가 잘려나갔습니다. 크롭 손실 0으로 두고
대신 우측 그리드 폭을 넓혀 균형을 맞췄습니다.

**모바일은 1열입니다.** 2열이면 타일이 158px까지 줄어 설비가 안 보입니다.

원본 PNG 4장 6.1MB → JPEG 413KB.

---

### 2. 임상 데이터 섹션 (`ClinicalData.tsx`)

세명대 화장품임상연구센터 성적서 2건의 수치를 차트 2개로 보여줍니다.

**처음엔 평균·표준편차만 받은 상태여서**, 피험자 10명 점을 평균 69.0 / 표준편차 9.4를
정확히 재현하도록 만든 **분포 예시**로 넣고 그렇다고 명시해뒀습니다.

**이후 성적서 PDF 원본을 찾아 전부 실측값으로 교체했습니다.**
PDF에 암호가 걸려 있어 `pdfjs-dist` 로 열었고, Appendix I 에 원문 표가 있었습니다.
검산 결과 평균 69.03(→69.0), 표준편차 9.40, 평균 ITA 49.3° 로 요약값과 일치했습니다.

받은 원고의 시험번호 `SMC-260731-9090 EN`(띄어쓰기)은 오타였고, 원문은 `_EN`(언더바)입니다.

**아직 모식도인 것 — UVA 흡광 곡선.**
성적서에 플레이트별 임계파장(λc)만 있고 흡광 스펙트럼 원본이 없습니다.
그래서 곡선은 λc 377.6nm 조건(290→377.6 면적 = 290→400 전체의 90%)으로 역산한
모식도이며, **화면에도 "모식도"라고 적어두고 실측 4장 수치는 표로 따로** 보여줍니다.
분광광도계 원본이 생기면 `CURVE_PATH` 상수 하나만 바꾸면 됩니다.

**차트는 라이브러리를 안 썼습니다.** 보여줄 게 숫자 두 개라서, 범용 차트를 쓰면
축·범례·격자가 딸려와 페이지 톤과 싸웁니다.

**왼쪽 카드가 비어 보이던 문제** — 오른쪽 카드에 표가 생기면서 높이가 벌어졌습니다.
성적서에 있던 ITA° 구간 분포표를 넣어 채웠습니다. 여백 메우기용이 아니라
ISO 24444 패널 요건을 충족했다는 근거라 바이어 실사팀이 보는 항목입니다.

---

### 3. 공인 인증서 섹션 (`Certifications.tsx`)

서류 5종, 클릭하면 라이트박스로 크게 열립니다.

**ISO 인증서 원본이 90° 돌아가 있었습니다.** 회전 4방향을 다 뽑아 비교해 바로잡았습니다.

**5장 비율이 제각각(1.33 / 1.41 / 1.42 / 1.49)이라 A4로 통일했습니다.**
잘라내지 않고 흰 여백을 덧대는 방식(1500×2121)이라 손실이 없고, 타일 5개가
똑같이 채워집니다. 재변환은 `node scripts/build-certs.mjs`.

**PDF 다운로드 버튼은 파일이 실제로 있을 때만 뜹니다.**
`public/docs/` 에 PDF가 없으면 버튼 자체가 렌더되지 않습니다(HEAD 요청으로 확인).
깨진 링크를 5개 뿌리는 것보다 낫습니다. 현재 인증서 PDF는 없는 상태입니다.

**⚠️ 명의가 셋으로 갈립니다 — 확인 필요**

| 서류 | 명의 |
|---|---|
| ISO 22716 | 주식회사 지디엠 (충북 청주) |
| 제조업 등록필증 | 에스엘코스메틱(주) (경기 시흥) |
| 기업부설연구소 | 에스엘코스메틱(주) |
| 특허 | 개인 (이인철) |
| 성적서 의뢰인 | GDM Co., Ltd. |

바이어 실사팀이 반드시 확인하는 부분입니다. 임의로 하나로 합치지 않고
라이트박스에 **"명의" 항목을 따로 만들어 그대로 표시**했습니다.
(특허증은 이인철, 등록필증 대표는 이민철인데 생년월일이 둘 다 700508로 같습니다)

---

### 4. 이날 잡은 버그

**① 가로 캐러셀이 로드 직후 24px 밀림**
`padding` 으로 여백을 주면 스크롤 범위가 화면 끝까지 남아 있어서 스냅이 레일을
자기 여백만큼 밀어냅니다. `margin` 으로 바꿔 해결. 리뷰 캐러셀에서 이미 겪은 문제입니다.

**② 이미지 로드가 한 번 실패하면 영구히 자리표시로 고정**
배포 직후 확인했더니 인증서 5장이 전부 회색으로 떴습니다. 파일은 200으로 정상 응답하는데도요.
CDN 엣지가 차가울 때 첫 요청이 실패하면 `failed` 상태가 고정되는 구조였습니다.
새로고침하면 정상이지만 **처음 들어온 방문자는 그 세션 내내 인증서를 못 봅니다.**
인증서를 보여주는 게 목적인 페이지라 치명적이라 고쳤습니다.

400ms/800ms 백오프로 2회 재시도 후에만 자리표시로 넘어갑니다. 두 방향 다 검증:
- 첫 요청을 강제로 끊음 → `?r=1` 로 복구, 자리표시 0개
- 진짜 없는 파일 → 정확히 3번만 시도하고 중단 (무한 재시도 없음)

재시도 횟수를 state가 아니라 ref에 둔 이유: setState 갱신함수 안에서 타이머를 걸면
StrictMode에서 두 번 실행됩니다.

배포 직후 라이브 확인 결과 **5장 전부 `?r=1` 로 로드**됐습니다. 이번에도 콜드 엣지
실패가 났고 재시도가 다 잡아냈습니다.

---

### 5. 리뷰 영상 1번째 (2번 슬롯)

받은 `동영상 1.mp4` 를 2번 자리에 넣었습니다. 1080×1920 9:16 / 18초로 규격은 맞았지만
**그대로 넣으면 안 되는 문제가 두 개** 있었습니다.

**① 코덱이 H.265(hvc1)였습니다.** 아이폰 기본 저장 포맷인데 크롬·파이어폭스에서
재생이 안 됩니다. `VideoGrid` 는 재생 실패를 감지해 자리표시로 바꾸므로,
"영상이 안 뜬다"가 아니라 **"조용히 회색 카드가 된다"** 로 나타났을 겁니다. → H.264로 재인코딩.

**② HDR(HLG / BT.2020)이었습니다.** 톤매핑 없이 변환하면 일반 모니터에서 색이 바랩니다.
변환 전후 프레임을 나란히 놓고 확인했더니 차이가 뚜렷해서, BT.709 SDR로 톤매핑했습니다.

**해상도는 810×1440으로 낮췄습니다.** 카드가 화면에서 제일 클 때가 384px라 1080은
용량만 두 배가 됩니다. CRF 23(4.47MB)과 26(3.13MB)을 제품 라벨 글씨가 보이는
1:1 크롭으로 비교했더니 육안 차이가 없어 **26으로 확정**했습니다. 최종 3.28MB.

같은 작업을 나머지 7개에도 해야 하므로 스크립트로 만들었습니다:
`npm run video -- "<원본파일>" <슬롯번호>`

**썸네일(poster) 지정 기능을 넣었습니다.** 원래 `VideoCard` 에 poster가 없어서 재생 전
카드가 비어 보였습니다. 요청하신 컷에 맞춰 3.4~4.1초를 뽑아 비교했고 — 4.1초는 눈을
감고 있어서 — **3.7초**로 정했습니다. 변환 스크립트가 썸네일도 같이 뽑습니다.

**소리는 "문제 없다"고 하셔서 그대로 뒀습니다.** 참고로 원인은 모달 영상의 `loop` 로,
18초마다 배경음악이 다시 시작됩니다. 카드는 음소거라 조용하고, 모달을 닫으면 소리는
정상적으로 멈춥니다(확인함). 거슬리면 `VideoGrid.tsx` 모달 `<video>` 에서 `loop` 만 빼면 됩니다.

**⚠️ 계정명이 지어낸 것입니다.** 2번 카드에 실제 인물이 나오는 영상이 들어갔는데
표시되는 계정은 `@sunny.beautylog` 로 자리표시입니다. 실제 촬영자 계정으로
바꿔야 합니다. 문구는 영상 내용에 맞게 "하이스킨 데일리 선크림 제품 소개"로 고쳤습니다.

---

### 6. 성분 이미지 4장 (아코디언)

Higgsfield MCP(Nano Banana 2, 2K, 4:3)로 생성했습니다. 톤은 보내주신 Origin·Safety
레퍼런스에 맞춰 **하이키 백색 배경 + 매크로 소재 컷 + 인물·텍스트·패키지 없음**으로 잡았습니다.

| 파일 | 성분 | 화면 |
|---|---|---|
| `img-04` | UV Filter & Titanium Dioxide | **연핑크 크림 스와치** — 유리 위 얇은 막, 빛 굴절 |
| `img-05` | Glutathione & Niacinamide | 진주빛 표면 위 투명 세럼 방울, 광채 하이라이트 |
| `img-06` | Hybrid UV Shield (텍스처) | 늘어나는 젤 가닥 — THE FORMULA 01 |
| `img-07` | (미사용) | 트러플 컷 — 없는 성분이라 카피 리뉴얼 때 제외 |

**연핑크는 img-04에 넣었습니다.** 두 가지 이유입니다 — 제형 컬러를 가장 직접적으로
보여주는 게 자외선 차단 크림 스와치이고, 아코디언이 **1번 항목을 기본으로 열어두기**
때문에(`useState(0)`) 섹션에 들어오는 순간 브랜드 컬러가 먼저 보입니다.
다른 항목으로 옮기고 싶으면 말씀만 주세요.

원본 PNG 4장 18.7MB → JPEG 699KB (2400×1792, q82).

---

### 7. 캐시 버그 — 새 이미지가 안 보이던 진짜 원인

성분 이미지를 배포했는데 **"안 들어가 있는데?"** 라는 피드백을 받고 확인해보니,
서버는 200으로 정상 응답하는데 브라우저에서만 404가 나고 있었습니다.

원인은 제가 넣어둔 `netlify.toml` 캐시 설정이었습니다.

```
HTTP/1.1 404 Not Found
Cache-Control: public, max-age=604800   ← 404 응답에도 붙음
```

**Netlify는 헤더 규칙을 404에도 적용합니다.** 이 프로젝트는 이미지를 하나씩 채워
넣는 구조라 아직 안 넣은 파일은 404인데, 그 404가 **7일간 브라우저에 저장**됩니다.
결과적으로 **자리표시 시절에 사이트를 본 사람일수록** 나중에 올린 진짜 이미지를
못 보게 됩니다. 이미 본 사람만 못 보는, 가장 나쁜 형태의 버그였습니다.

앞서 인증서 5장이 회색으로 떴던 것도 같은 원인이었습니다.
그때 "CDN 엣지가 차가워서"라고 진단했는데 **틀린 진단**이었고,
그래서 넣은 재시도 로직도 근본 해결이 아니었습니다
(같은 404가 캐시되어 있으니 `?r=1`, `?r=2` 도 결국 다 실패).

**수정** — 10분 + 재검증으로 낮췄습니다.

```toml
Cache-Control = "public, max-age=600, must-revalidate"
```

바뀌지 않은 파일은 304(내용 전송 없음)로 끝나서 사실상 공짜이고,
새로 올린 파일은 **10분 안에 모두에게 보입니다.** 이 규모의 B2B 페이지에서
아낄 수 있는 트래픽보다 "올렸는데 안 보인다"를 없애는 쪽이 훨씬 중요합니다.
`/videos/*` 에도 같은 규칙을 추가했습니다. 나중에 모든 이미지가 확정되면 다시 올려도 됩니다.

> **이미 옛날 404가 캐시된 브라우저는** 강력 새로고침(`Ctrl`+`Shift`+`R`)이 한 번 필요합니다.
> 이번 수정 이후 추가되는 파일부터는 그냥 기다리면 보입니다.

---

### 8. 남은 것

- [ ] **문의 폼 전송** — 화면상 성공 표시만 뜨고 실제로 메일이 가지 않습니다.
      **거래처에 주소를 알리기 전에 반드시 처리해야 합니다.** (`InquiryForm.tsx` 의 `handleSubmit`)
- [ ] 인증서 PDF 5개 — 없음. 생기면 `public/docs/` 에 넣으면 버튼이 자동으로 붙습니다
- [ ] UVA 흡광 곡선 원본 데이터 (위 2번 참고)
- [x] ~~`img-04` ~ `img-07` 성분 이미지~~ — 완료 (AI 생성)
- [ ] `img-02`, `img-03`, `img-08` ~ `img-10` — 아직 자리표시
- [ ] 리뷰 영상 7개 + **계정명·문구** — 2번 슬롯만 실제 영상입니다
- [ ] 페이지에서 문구를 직접 고치는 편집 모드

---

## 다음 세션 시작할 때

### 저장소

**https://github.com/sjjw2526-commits/hiskin-ver2-final** (Private 🔒)

⚠️ **구버전과 헷갈리지 마세요.** `sjjw2526-commits/hiskin` 은 v1 사이트입니다.
이어서 작업할 건 이름에 `-ver2-final` 이 붙은 쪽입니다.

### 회사 PC에서 처음 세팅한다면

```bash
gh auth login        # GitHub 로그인 (브라우저 인증, 1회)
git clone https://github.com/sjjw2526-commits/hiskin-ver2-final.git
cd hiskin-ver2-final
npm install          # 의존성 설치
npm run dev          # http://localhost:3000
```

배포까지 하려면 한 번 더:

```bash
npx netlify login    # 브라우저 인증, 1회
npx netlify link     # hiskin 사이트에 연결
npm run deploy       # 이후엔 이 한 줄
```

### 작업 끝낼 때

```bash
git add -A && git commit -m "내용"
git push             # ← 이걸 빠뜨리면 다른 PC에서 못 받습니다
```

### 바로 이어서 할 일 (우선순위 순)

1. 🔴 **히어로 `img-01` 의 `SPF 90` 표기** — 실제는 SPF 50+ 입니다. 최상단 제일 큰
   이미지에 잘못된 스펙이 박혀 있어, 거래처에 주소를 알리기 전에 반드시 교체해야 합니다.
2. 🔴 **문의 폼 전송 연결** — 지금은 폼을 채워 보내도 화면에만 "성공"이 뜨고
   **아무 데도 안 갑니다.** Netlify 에 올라가 있으니 Netlify Forms 로 붙이면 빠릅니다.
   받을 메일 주소만 있으면 됩니다. (`InquiryForm.tsx` 의 `handleSubmit`)
3. 🔴 **리뷰 계정명·문구 정리** — 8개 전부 지어낸 자리표시입니다. AI 생성 영상이
   실제 고객 후기처럼 보이는 상태이기도 해서, 실촬영만 후기로 두거나 AI 영상은
   브랜드 영상으로 분리하는 등 정리가 필요합니다.
4. 🟡 **모바일 히어로 트리오** — 폰에서 세로 띠 3개가 됩니다(칸당 약 122×820px).
   의도한 디자인인지 판단하고, 아니면 모바일에서 열 수를 줄이거나 가로 스크롤로 전환.
5. 🟡 **리뷰 영상 01·03 길이** — 각각 19MB, 22MB (70초 초과). 15~20초로 자르면 3~4MB.
6. 🟡 **`img-08`** — 마지막 남은 자리표시. `Downloads/제품 정면 샷.jpg` 실물을 그대로
   넣으면 됩니다. **이 자리는 라벨이 읽혀야 하므로 AI 생성 금지.**
7. 인증서 명의 3사 분리 건 — 표기 방식 확정 필요
8. 인증서 PDF 5개 — `public/docs/` 에 넣으면 다운로드 버튼이 자동으로 붙습니다

---

## 참고

- 프로젝트 전반·배포·수정 방법 → [`README.md`](README.md)
- 이미지 파일명·규격 → [`public/images/README.md`](public/images/README.md)
- 인증서 PDF 넣는 법 → [`public/docs/README.md`](public/docs/README.md)
- **스크롤 모션은 건드리기 전에 README 7번 항목을 꼭 읽으세요.** 서로 맞물려 있어서
  이유를 모르고 고치면 예전에 잡은 문제가 되살아납니다.
