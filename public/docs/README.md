# 인증서 PDF

아래 이름으로 이 폴더에 넣으면 인증서 라이트박스에 **Download PDF** 버튼이 자동으로 생깁니다.

- `cert_iso22716.pdf`
- `cert_mfg_license.pdf`
- `cert_patent.pdf`
- `cert_rnd_lab.pdf`
- `cert_irdop_analysis.pdf`

파일이 없으면 버튼이 아예 표시되지 않습니다 (깨진 링크 방지).
파일명은 `src/components/Certifications.tsx` 의 `DOCS` 배열에서 바꿀 수 있습니다.
